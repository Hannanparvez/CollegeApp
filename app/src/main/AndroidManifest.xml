<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.com.ssmguide.ssmguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".OnLearning"></activity>
        <activity android:name=".OnDepartmentActivity" >


    </activity>
        <activity android:name=".OnFacilities" />
        <activity android:name=".LibraryActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".LearningWindow">


        </activity>


        <activity android:name=".AdmissionActivity" />
        <activity android:name=".StaffhomeActivity" />
        <activity android:name=".MapsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".ResultsActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".DepartmentActivity" >

        </activity>
        <activity android:name=".FacilitiesActivity" />
        <activity android:name=".StudenthomeActivity">

        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".SigninActivity">

        </activity>
    </application>

</manifest>